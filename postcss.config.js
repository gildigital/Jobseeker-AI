// In your root directory (jobseeker-ai-app)
// Create a file named postcss.config.js with the following content:

module.exports = {
    plugins: {
      '@tailwindcss/postcss': {},
      autoprefixer: {},
    },
  }